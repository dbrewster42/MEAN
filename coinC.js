function coinC(n){
	let dic = { dollars: 100;
		quarters : 25;
		dimes : 10;
		nickels : 5;
		pennies : 1;
	}

	// arr = [100, 25, 10, 5, 1]
	var newDic = {};
	for (let i = 0; i < dic.length; i++){
			if (var n > dic.val[i])
				count = 0;
				for (n = n; n > dic.val[i]; n = n - arr[i]){
					count ++;
					newDic['dic.key'] = count;
				}


}
	if n > 100
	let obj = { dollars: i}
	dollars = n / 100;
	quarters = n
	for (let key in obj){
		if(obj.hasOwnProperty(key)){
			console.log(`${key} : ${obj[key]}`)
		}
	}

}
